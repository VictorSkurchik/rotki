# Use native design systems with shared semantics

Android will use Material 3 conventions and iOS will use native SwiftUI and Apple interaction conventions rather than share or imitate identical components. The Clients will share Rotki semantic colors, icon meanings, terminology, and rules for presenting amounts, freshness, errors, privacy, and loading states, but pixel parity is explicitly not a goal.
